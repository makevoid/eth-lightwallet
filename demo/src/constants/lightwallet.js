'use strict';

import { createConstants  } from '../utils';

export default createConstants(
    'CREATE_LIGHTWALLET_PASSPHRASE',
    'CREATE_LIGHTWALLET',
    'LOAD_LIGHTWALLET',
    'SAVE_LIGHTWALLET',
    'SEND_ETHER_FROM_LIGHTWALLET'
);
